import type { Player, Tag, Match } from '../types';
import { ActionType, ActionResult } from '../types';

export const mockPlayers: Player[] = [];

export const mockTags: Tag[] = [];

export const generateMockData = (): Match[] => {
    return [];
};
